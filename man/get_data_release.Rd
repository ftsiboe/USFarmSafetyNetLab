% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{get_data_release}
\alias{get_data_release}
\title{Download and cache a data release RDS file from the US-FarmSafetyNet-Lab GitHub repository}
\usage{
get_data_release(name = "sobscc", year = "all")
}
\arguments{
\item{name}{Character. Base name of the data release (one of "sobscc", "sobcov", "sobtpu", or "colsom"). Defaults to "sobscc".}

\item{year}{Character or numeric. Year of the data release (e.g., 2020) or "all" for combined releases across all years.
Defaults to "all".
Availability:
* sobscc: "all" only
* sobcov: "all" and individual years from 1989–2025
* sobtpu: "all" and individual years from 1999–2025
* colsom: "all" and individual years from 1989–2025}
}
\value{
The R object loaded from the requested RDS file.
}
\description{
This function constructs the filename \verb{<name>_<year>.rds}, determines the appropriate GitHub release tag
based on the prefix of \code{name} ("sob" or "col"), and downloads the file via the piggyback package
if it is not already present in the user’s cache directory. The file is then read into R and returned.
Currently supports only Summary of Business releases ("sobscc", "sobcov", "sobtpu") and Cause of Loss releases ("colsom").
}
\details{
\itemize{
\item \code{tools::R_user_dir("USFarmSafetyNetLab", which = "cache")} locates or creates a per-user cache directory.
\item \code{piggyback::pb_download()} fetches the file from the GitHub repo "ftsiboe/US-FarmSafetyNet-Lab" under the appropriate tag.
\item Subsequent calls with the same name and year retrieve the cached file, avoiding repeated downloads.
}
}
\examples{
\dontrun{
  # Download or retrieve cached "sobscc_2021.rds"
  df <- get_data_release("sobscc", 2021)
}
}
