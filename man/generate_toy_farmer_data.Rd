% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_toy_farmer_data.R
\name{generate_toy_farmer_data}
\alias{generate_toy_farmer_data}
\title{Generate a sample farmer-level toy dataset for testing}
\usage{
generate_toy_farmer_data(
  n = 100,
  year = 2020,
  state = 18,
  counties = 7:23,
  commodity = 41,
  types = c(26, 16),
  practices = c(2, 3, 997),
  unit_codes = c("BU", "OU", "EU"),
  plan_codes = c(1:6, 16:17, 90),
  coverage_codes = c("A", "C"),
  cov_level_range = c(0.5, 0.95),
  yield_mean = 100,
  yield_sd = 10,
  price_mean = 2.5,
  price_sd = 1.5,
  price_share = NULL,
  area_share = NULL,
  planted_area = NULL,
  production_history_length = 0:10
)
}
\arguments{
\item{n}{Integer number of farmer records to generate. Default 100.}

\item{year}{Numeric value of commodity year. Default 2020.}

\item{state}{Numeric vector of state FIPS codes. Default 19.}

\item{counties}{Numeric vector of county FIPS codes. Default 127.}

\item{commodity}{Numeric vector of commodity codes. Default 41.}

\item{types}{Numeric vector of type codes. Default 16.}

\item{practices}{Numeric vector of practice codes. Default c(2, 3, 997).}

\item{unit_codes}{Character vector of unit structure codes. Default c("BU", "OU", "EU").}

\item{plan_codes}{Numeric vector of insurance plan codes. Default c(1:6, 16:17, 90).}

\item{coverage_codes}{Character vector of coverage type codes. Default c("A", "C").}

\item{cov_level_range}{Numeric vector of length 2 giving the min and max for coverage_level_percent. Default c(0.8, 0.8).}

\item{yield_mean}{Numeric mean for approved_yield and rate_yield. Default 100.}

\item{yield_sd}{Numeric standard deviation for approved_yield and rate_yield. Default 10.}

\item{price_mean}{Numeric mean for expected price. Default 2.5.}

\item{price_sd}{Numeric standard deviation for expected price. Default 1.5.}

\item{price_share}{Numeric}

\item{area_share}{Numeric}

\item{planted_area}{Numeric}

\item{production_history_length}{Integer vector of indices for actual_farm_yield columns. Default 0:10.}
}
\value{
A \code{data.table} with one row per farmer and columns:
\itemize{
\item \code{farmid}
\item \code{commodity_year}
\item \code{state_code}, \code{county_code}
\item \code{commodity_code}, \code{type_code}, \code{practice_code}
\item \code{unit_structure_code}, \code{insurance_plan_code}
\item \code{coverage_type_code}, \code{coverage_level_percent}
\item \code{approved_yield}, \code{rate_yield}
\item \code{reported_acres}, \code{insured_share_percent}
\item \code{price_election_percent}, \code{dmage_area_rate}, \code{market_year_price}
\item \code{actual_farm_yield_0} to \code{actual_farm_yield_<max(production_history_length)>}
}
}
\description{
Generate a sample farmer-level toy dataset for testing
}
\examples{
farmdata <- generate_toy_farmer_data()
farmdata <- generate_toy_farmer_data(
  n = 20,
  cov_level_range = c(0.7, 0.9),
  production_history_length = 0:2
)
head(farmdata)

}
