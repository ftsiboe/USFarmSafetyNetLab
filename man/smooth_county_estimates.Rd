% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_county_estimates.R
\name{smooth_county_estimates}
\alias{smooth_county_estimates}
\title{Smooth county-level estimates via spatial contiguity}
\usage{
smooth_county_estimates(
  data,
  fip_col = "fip",
  estimate_col = "estimate",
  iterations = 5,
  fun = mean,
  n_classes = 10
)
}
\arguments{
\item{data}{A data.frame or tibble containing at least two columns:
- a 5-digit FIPS code (character)
- a numeric estimate to be smoothed}

\item{fip_col}{Name of the FIPS-code column in \code{fit_df} (default 'fip').}

\item{estimate_col}{Name of the numeric estimate column in \code{fit_df} (default 'estimate').}

\item{iterations}{Number of smoothing passes to perform (default 5).}

\item{fun}{A function to aggregate neighbor values; must take a numeric vector and return a single numeric (default mean).}

\item{n_classes}{Number of categories for Jenks natural-breaks classification (default 10).}
}
\value{
An \strong{sf} object of all U.S. counties with these new columns:
\itemize{
\item estimate: your original values (NA where missing)
\item estimate_smooth: the same values after neighborhood-function imputation
\item estimate_cat: factor giving the Jenks-break category
}
}
\description{
This function
\enumerate{
\item Loads all U.S. county geometries via \strong{urbnmapr}
\item Joins in a user-supplied data.frame of county estimates (by 5-digit FIPS)
\item (Optionally) converts to a \strong{terra} SpatVector for further terra ops
\item Builds a contiguity neighbor list using \strong{sf::st_touches}
\item Iteratively fills any missing estimates by applying a user-supplied function to neighbor values
\item Classifies the smoothed values into \code{n_classes} using Jenks natural breaks
}
}
