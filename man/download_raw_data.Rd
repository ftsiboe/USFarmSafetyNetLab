% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{download_raw_data}
\alias{download_raw_data}
\title{Download and cache raw data files}
\usage{
download_raw_data(dir_fastscratch = "./data-raw/fastscratch")
}
\arguments{
\item{dir_fastscratch}{\code{character(1)}
Path to a directory where downloaded files will be stored.  Defaults to \code{"./data-raw"}.}
}
\value{
Invisibly returns \code{NULL}. Side effects include creating files under \code{./data-raw}.
}
\description{
Ensures that a \code{./data-raw} directory exists, and for each requested data type,
downloads the corresponding dataset (if not already present) and saves it as an RDS file.
}
\details{
\itemize{
\item If the \code{./data-raw} directory does not exist, it will be created.
\item For \code{"sobtpu"}:
\itemize{
\item Checks for an existing \code{sobtpu.rds} file in \code{./data-raw}.
\item If missing, calls \code{rfcip::get_sob_data(sob_version="sobtpu", year=1999:current_year)}
to retrieve the data, coerces it to a \code{data.table}, and saves it to \code{./data-raw/sobtpu.rds}.
}
}
}
\examples{
\dontrun{
  # Download only the SOBTPU data if not already cached
  download_raw_data("sobtpu")

  # Download multiple data types (future-proof)
  download_raw_data(c("sobtpu"))
}

}
