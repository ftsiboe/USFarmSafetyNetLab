% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_fcip_data.R
\name{prep_fcip_data}
\alias{prep_fcip_data}
\title{Prepare FCIP Data for Release}
\usage{
prep_fcip_data(dir_dest = "data-raw/data_release")
}
\arguments{
\item{dir_dest}{Character. Base directory under which to store processed FCIP data.
Defaults to \code{"data-raw/data_release"}. Subdirectories \code{sob}, \code{col}, and \code{adm}
will be created within \code{dir_dest} if they do not already exist.}
}
\value{
A list of three character vectors, naming the files saved in:
\itemize{
\item \code{dir_dest/sob}
\item \code{dir_dest/col}
\item \code{dir_dest/adm}
}
}
\description{
Downloads, processes, and caches multiple Federal Crop Insurance Program (FCIP)
datasets—Summary of Business (SOB), Cause of Loss (COL), and Actuarial Data Master (ADM)—
saving them as RDS files in a local cache directory structure.
}
\details{
\enumerate{
\item \strong{Determine source paths}
\itemize{
\item On Windows, locates the \code{farmpolicylab} database under the user's OneDrive→Dropbox folder.
\item On non-Windows, uses \verb{~/Database/USA/USDA/}.
}
\item \strong{Setup local cache directories}
\itemize{
\item Ensures \code{dir_dest} exists.
\item Creates subfolders \code{sob}, \code{col}, and \code{adm} if missing.
}
\item \strong{Summary of Business (SOB)}
\itemize{
\item Downloads yearly CSV ZIPs via \code{download_rma_web_data_files()} for \code{sobtpu}
and \code{sobcov}, plus field description PDFs.
\item Unzips and reads the historic 1948–1988 SOB data, coerces selected columns to
numeric, and saves as \code{sobscc_1948_1988.rds}.
}
\item \strong{Cause of Loss (COL)}
\itemize{
\item Downloads yearly CSV ZIPs for \code{colsom}, plus field description PDF and
Excel code listing.
\item Unzips and reads historic 1948–1988 COL data, coerces selected columns
(including \code{indemnity_amount}) to numeric, computes \code{loss_ratio}, filters
invalid rows, and saves as \code{col_sob_hist.rds}.
}
\item \strong{Actuarial Data Master (ADM)}
\itemize{
\item Reads RDS files under \verb{rmaFCIPdata/.../base_rate/}, computes \code{tau_adm = Rr + Rf},
averages by \verb{(crop_yr, state_cd, county_cd, crop_cd)}, and saves
\code{fcip_demand_instruments_from_adm.rds}.
\item Reads ERS price projections RDS, renames price columns, averages by
\verb{(state_cd, county_cd, typ_cd, crop_yr, crop_cd)}, standardizes column names,
and saves \code{fcip_commodity_prices.rds}.
}
}
}
