% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_sobtpu_with_covariates.R
\name{enrich_with_program_age}
\alias{enrich_with_program_age}
\title{Attach "program age" by (state, county, commodity, year)}
\usage{
enrich_with_program_age(sob, sobcov = NULL, sobscc = NULL)
}
\arguments{
\item{sob}{data.frame/data.table with (required) columns:
\code{state_code}, \code{county_code}, \code{commodity_code}, \code{commodity_year}.}

\item{sobcov}{Optional data.frame/data.table for SOBCOV; if \code{NULL}, it is downloaded.
Must contain \code{commodity_year, commodity_code, state_code, county_code,
  net_reported_quantity, liability_amount}.}

\item{sobscc}{Optional data.frame/data.table for SOBSCC; if \code{NULL}, it is downloaded.
Same column expectations as \code{sobcov}.}
}
\value{
data.table equal to \code{sob} with new columns:
\code{program_age_by_year}, \code{program_age_by_year_10},
\code{program_age_by_year_05}, \code{program_age_by_year_04}.
}
\description{
Computes, for each row's (state_code, county_code, commodity_code, commodity_year),
the \strong{span of years observed prior to the current year} with positive liability
in the combined SOBCOV/SOBSCC aggregates:
}
\details{
\deqn{ program\_age\_by\_year(y) = \max\{t<y\} - \min\{t<y\}, \quad
       t \in \text{years with liab\_actual > 0} }

If there is no prior year with positive liability, the age is set to 0.
Binned variants at widths 10/5/4 years are also added.
\itemize{
\item Uses efficient cumulative min/max over a union timeline of \strong{target years} (from \code{sob})
and \strong{observed positive-liability years} (from SOBCOV/SOBSCC) per group.
\item All joins are left joins; no rows are dropped from \code{sob}.
\item Age is computed on the year scale (integers) and \strong{does not add 1} to the span,
matching your original specification (\eqn{\max - \min}).
}
}
