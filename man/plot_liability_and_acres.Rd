% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_helpers.R
\name{plot_liability_and_acres}
\alias{plot_liability_and_acres}
\title{Plot Liability and Net Reported Acres Faceted by Disaggregation Variable}
\usage{
plot_liability_and_acres(
  data,
  disag,
  time_scale_theme = NULL,
  general_theme = NULL
)
}
\arguments{
\item{data}{A \code{data.frame} containing the columns: \code{crop_year}, \code{value}, \code{variable},
and a column specified by \code{disag}. The \code{variable} column should include
"(A) Liability in U.S. dollars" and "(B) Net reported acres" as possible values.}

\item{disag}{A \code{character} string specifying the name of the column in \code{data}
to be used for grouping in the plots (e.g., \code{"commodity"}, \code{"state"}).}

\item{time_scale_theme}{An optional ggplot2 theme object controlling time axis styling.
If \code{NULL}, the function expects a globally defined \code{myTime} theme.}

\item{general_theme}{An optional ggplot2 theme object for general plot styling.
If \code{NULL}, the function expects a globally defined \code{myTheme}.}
}
\value{
A \code{gtable} object combining:
\itemize{
\item Panel A: Liability by year and group
\item Panel B: Net reported acres by year and group
\item Shared x-axis label
\item Combined legend at the bottom
}
}
\description{
This function creates a two-panel bar chart that visualizes (A) liability in U.S. dollars
and (B) net reported acres over time, grouped by a user-defined disaggregation variable.
A shared x-axis and a unified bottom legend are included.
}
\details{
The function uses \code{ggplot2}, \code{gridExtra}, and \code{gtable} for construction and layout.
You can pass custom themes to override the defaults.
}
\examples{
\dontrun{
plot_liability_and_acres(
  data = data_comm, 
  disag = "commodity",
  time_scale_theme = theme(axis.text.x = element_text(angle = 45)),
  general_theme = theme_minimal()
)
}

}
